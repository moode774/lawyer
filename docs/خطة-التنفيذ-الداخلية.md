# خطة التنفيذ الداخلية — محمد (لا تُعرض على المحامي)
مرجع: تقرير الفحص `AUDIT-REPORT.md` + فحص قاعدة Supabase الحية 2026-07-31.

---

## المرحلة 0 — التجهيز والقرارات (أسبوع)

**غير تقني (مع المحامي):**
- [ ] اعتماد الخطة الرسمية ونطاق العمل، وفتح نقاش المقابل المالي وآلية التعاون (شهري / مراحل / مزيج).
- [ ] شريحة/رقم جوال مخصص للمكتب (واتساب + استقبال العملاء) — لا يُستخدم رقم شخصي.
- [ ] بريد مهني على نطاق المكتب (بعد شراء الدومين).
- [ ] جمع المتطلبات النظامية: السجل التجاري، ترخيص المحاماة، الحساب البنكي للمكتب، هل المكتب مسجل في ضريبة القيمة المضافة؟ (يحدد مسار الفوترة).
- [ ] مقارنة برنامجي محاسبة معتمدين من زاتكا (قيود / وافق) واختيار واحد — معيار الاختيار: السعر، سهولة الفوترة الإلكترونية، تقارير VAT.
- [ ] تنزيل WhatsApp Business (المجاني) على الرقم المخصص: ملف تجاري كامل، رسالة ترحيب، ردود سريعة (استقبال طلب، طلب مستندات، تأكيد موعد)، تصنيفات (عميل محتمل / عميل / موعد / متابعة).

**تقني (إعداد خارجي في Supabase Dashboard):**
- [ ] تفعيل مزود SMS في Auth → Phone (يُنصح بمزود يدعم السعودية جيدًا) — بدونه دخول العملاء لا يعمل.
- [ ] ضبط `OPENAI_API_KEY` في Edge Function Secrets.
- [ ] تفعيل Leaked Password Protection في Auth (تحذير الفاحص الأمني).
- [ ] حذف `VITE_OPENAI_API_KEY` و`VITE_OPENROUTER_API_KEY` من `.env` وتدوير المفاتيح إن كانت حقيقية.

## المرحلة 1 — تشغيل المنصة فعليًا (2–3 أسابيع)

**إصلاحات أمنية (بالترتيب):**
- [ ] `auth.tsx`: قلب الافتراض — الدور الافتراضي `client`، ولا يدخل `/admin` إلا من دوره في profiles ضمن (super_admin/lawyer/staff/marketing).
- [ ] Migration: قيد FK يربط `profiles.id` بـ `auth.users(id) ON DELETE CASCADE`.
- [ ] Migration: عمود `clients.user_id UUID REFERENCES auth.users(id)` + استبدال سياسات العميل المبنية على البريد بسياسات على `user_id` (البريد الوهمي `@phone.local` يجعل سياسات البريد عديمة الفائدة).
- [ ] نشر Edge Function `generate-marketing-image` (غير منشورة إطلاقًا حاليًا) مع إضافة فحص داخلي: JWT صالح + دور staff قبل استدعاء OpenAI.

**ربط البيانات (إزالة الطبقة التجريبية تدريجيًا):**
- [ ] clients: ربط كامل بـ Supabase (إنشاء من Lead داخل transaction، بلا fire-and-forget) + آلية ربط `user_id` عند أول دخول للعميل بمطابقة الجوال الموحد +9665.
- [ ] matters / tasks / documents / messages: CRUD كامل على Supabase.
- [ ] getLead / updateLeadNotes: تحويلها إلى Supabase (حاليًا محلية فقط).
- [ ] إزالة الـ fallback الصامت إلى LocalStorage في القراءات الحساسة — الفشل يظهر خطأً واضحًا.
- [ ] توحيد `messages.direction` مع قيد القاعدة (inbound/outbound).
- [ ] بوابة العميل: استبدال المعرف التجريبي `c_1` باستعلامات مقيدة بـ RLS (قضاياه، مستنداته visibility=client فقط، مواعيده، رسائله، ملفه).
- [ ] Dashboard وAnalytics: إحصاءات حقيقية من القاعدة بدل الأرقام الثابتة.
- [ ] رفع المستندات إلى Supabase Storage مع bucket خاص وسياسات وصول.
- [ ] بعد كل دفعة: `npm.cmd run build` + فحص RTL والجوال.

**إطلاق:** دومين + استضافة (Vercel أو مماثل) + ربط SSL + فحص شامل للنماذج الثلاثة على الإنتاج.

## المرحلة 2 — الحسابات والالتزام الضريبي (أسبوعان)
- [ ] تفعيل البرنامج المحاسبي المختار وربط بيانات المكتب الرسمية.
- [ ] شجرة حسابات مبسطة لمكتب محاماة: أتعاب استشارات، أتعاب قضايا، رسوم حكومية مستردة، رواتب، إيجار، اشتراكات تقنية، تسويق.
- [ ] دورة موحدة: عرض سعر → فاتورة إلكترونية → سند قبض → تسجيل التحصيل.
- [ ] سجل مصروفات أسبوعي مع أرشفة الفواتير المؤيدة.
- [ ] جلسة مع محاسب/مستشار زكاة وضريبة مرخص لاعتماد الوضع الضريبي ومواعيد الإقرارات (لا نجتهد في هذا).
- [ ] لاحقًا: تقييم ربط البرنامج المحاسبي بالمنصة عبر API أو بناء وحدة مالية داخلية.

## المرحلة 3 — واتساب والتسويق (2–3 أسابيع)
- [ ] تشغيل دورة واتساب كاملة: استفسار → تسجيله Lead في المنصة يدويًا (نموذج سريع في لوحة الإدارة) → متابعة بحالات الـ Pipeline.
- [ ] قوالب رسائل موحدة: استلام الطلب، تأكيد الموعد، تذكير قبل الموعد، طلب مستندات، إغلاق مهذب.
- [ ] عند نضج التشغيل: تقييم WhatsApp Business API عبر مزود معتمد (ربط آلي بالمنصة + إشعارات تلقائية) — لا نعد به قبل توفر الميزانية والتحقق من المتطلبات.
- [ ] تفعيل مركز التسويق على بيانات حقيقية فقط، وإبقاء كل ما هو تجريبي معلّمًا بوضوح.
- [ ] خطة محتوى شهرية (منشورات توعوية قانونية مهنية — بلا ضمان نتائج) + توليد الصور عبر الدالة المنشورة.
- [ ] قياس مصادر العملاء عبر `marketing_attribution` (UTM).

## المرحلة 4 — التقارير والأتمتة (مستمر)
- [ ] تقرير شهري موحد للمحامي: طلبات جديدة، معدل التحويل، قضايا نشطة، مواعيد، مصروفات/إيرادات (من البرنامج المحاسبي)، أبرز التوصيات.
- [ ] تنبيهات آلية: موعد غدًا، مهمة متأخرة، طلب بلا رد > 24 ساعة.
- [ ] نسخ احتياطي دوري + مراجعة سياسات RLS بعد أي migration.

---

## قواعد ثابتة طوال التنفيذ
1. لا بيانات تجريبية تُعرض كحقيقية، ولا fallback صامت في العمليات الحساسة.
2. كل تغيير على القاعدة عبر migration مسماة.
3. لا أسرار في VITE_* ولا service_role في المتصفح.
4. كل نص تسويقي/قانوني بصياغة مهنية بلا ضمان نتائج.
5. `npm.cmd run build` بعد كل تعديل.
