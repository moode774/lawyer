# تقرير الفحص الشامل — نظام إدارة مكتب المحاماة

التاريخ: 2026-07-28
المشروع: React 19 + Vite + TypeScript + Supabase (اسميًا)

---

## الخلاصة التنفيذية

النظام في حالته الحالية **واجهة عرض تجريبية (Demo) مصقولة بصريًا، وليس نظامًا منتجًا فعليًا**.
كل البيانات تعمل على `localStorage` في المتصفح، والمصادقة وهمية، وقاعدة بيانات Supabase
موجودة كملفات SQL فقط ولا يقرأ منها التطبيق أي سطر. التصميم والصفحات العامة ممتازة،
لكن الوظائف الجوهرية (البوابة، المستندات، المهام، الأتمتة، الذكاء الاصطناعي) معطّلة أو محاكاة.

**حالة البناء:** TypeScript يُبنى بنجاح ✅ | الفحص الآلي: خطأ واحد + 155 تحذيرًا ⚠️ | لا توجد اختبارات إطلاقًا ❌

---

## أولًا: ما يعمل فعليًا

- **الموقع التعريفي العام**: الرئيسية، الخدمات، تفاصيل الخدمة، الأسئلة الشائعة، من نحن،
  المقالات، الخصوصية، الشروط — محتوى ثابت كامل وجاهز من `src/data/demo.ts`.
- **نموذج حجز الموعد** `/book`: يعمل ويحفظ في localStorage مع رقم مرجعي.
- **نموذج الاستقبال القانوني** `/legal-intake`: معالج من 5 خطوات مع تحقق من المدخلات.
- **لوحة تحكم الأدمن — جزئيًا**: عرض العملاء المحتملين، تغيير مرحلة العميل في الـ Pipeline
  (بقائمة منسدلة)، تحويل عميل محتمل إلى عميل فعلي، إضافة ملاحظات، بحث عام (Ctrl+K).
- **حراس المسارات**: توجيه غير المسجلين إلى `/login` (حماية واجهة فقط، قابلة للتجاوز).
- **دعم عربي/إنجليزي + RTL**: مزوّد لغة يعمل (`src/lib/i18n.tsx`) مع حفظ التفضيل.

---

## ثانيًا: مشاكل حرجة (تمنع الإنتاج)

### 1. لا يوجد Backend فعلي — كل شيء localStorage
- `src/lib/store.ts` هو طبقة البيانات الوحيدة؛ البيانات تضيع عند مسح المتصفح ولا تُشارك بين الأجهزة أو الموظفين.
- دوال المزامنة مع Supabase (`createLead`, `createAppointment`, `createClientFromLead`)
  **كود ميت لا يستدعيه أي صفحة** (store.ts:167, 248, 297).
- لا يوجد في التطبيق كله ولا استعلام قراءة `select` واحد من Supabase.
- أعمدة الحجز المرسلة لـ Supabase لا تطابق المخطط (`appointment_type` مقابل `meeting_type` إلخ) — أي محاولة إدراج ستفشل حتى لو فُعّلت.
- أخطاء Supabase تُبتلع بصمت (`console.warn` فقط).
- يوجد رابط مشروع Supabase ثابت احتياطي مع مفتاح فارغ في `src/lib/supabase.ts:3-4`.

### 2. المصادقة وهمية بالكامل
- `src/lib/auth.tsx:48-52` يقبل أي بريد يحتوي `@` ويتجاهل كلمة المرور، ويسجّل الجميع
  كأدمن باسم ثابت «أ. أحمد المحامي».
- اختيار «بوابة العميل» في صفحة الدخول **يُتجاهل** — تدخل البوابة باسم المحامي (LoginPage.tsx:23).
- لا يوجد Supabase Auth، لا استعادة كلمة مرور، لا أدوار وصلاحيات حقيقية
  (الأدوار `lawyer/staff/marketing` في البيانات التجريبية غير مستخدمة).
- جدول `profiles` غير مربوط بـ `auth.users`، وبالتالي سياسة RLS الخاصة به لن تتطابق أبدًا.

a- معرّفات عميل ثابتة متناقضة: `_shared.tsx` يستخدم `'c2'` والصفحات تستخدم `'c_1'`،
  والمعرّفات الحقيقية تُولّد عشوائيًا — **كل صفحات البوابة فارغة دائمًا**.
- `PortalAppointmentsPage.tsx:17` يعرض مواعيد **كل العملاء** لأي مستخدم — تسريب بيانات بالتصميم.
- رسائل البوابة حالة React محلية فقط تضيع عند التحديث ولا تصل الأدمن (`PortalMessagesPage.tsx:15-26`).
- تحية ثابتة «شركة الحلول الرقمية المحدودة» بغض النظر عن المستخدم.
- أي مستخدم مسجل (حتى الأدمن) يستطيع دخول البوابة — لا فحص دور.

### 4. سياسات الأمان RLS ناقصة ومكسورة
- جداول `appointments` و`tasks` و`messages`: RLS مفعّل **بدون أي سياسة** = كل الوصول مرفوض.
- `marketing_attribution` و`automation_settings`: RLS غير مفعّل = مفتوحة للجميع.
- `auth.jwt() ->> 'role'` ادعاء غير قياسي في Supabase — السياسات لن تتطابق دون custom claims.

---

## ثالثًا: وظائف محاكاة/وهمية (تبدو تعمل ولا تعمل)

| الميزة | الحالة | المرجع |
|---|---|---|
| إرسال واتساب | يسجل «queued» محليًا فقط، لا إرسال أبدًا | `store.ts:72-86` |
| صفحة الإعدادات | زر الحفظ يستدعي دالة فارغة ويعرض «تم الحفظ بنجاح» | `store.ts:459` |
| مساعد الذكاء الاصطناعي | ردود جاهزة بمطابقة كلمات مفتاحية + `setTimeout`، لا LLM | `AiPage.tsx:30-65` |
| صفحة التحليلات | أرقام ثابتة مختلقة («14,250»، «4.8x») غير مربوطة بالبيانات | `AnalyticsPage.tsx:20-34` |
| شارة الإشعارات | تعرض «3» وهمية وإشعارًا مزيفًا عند عدم وجود إشعارات | `AdminLayout.tsx:143,183` |
| نسبة التحويل | ثابتة `68%` | `store.ts:449` |
| نموذج اتصل بنا | يرسل إلى لا شيء — الرسالة تُفقد بصمت | `ContactPage.tsx:18-21` |
| رفع المستندات | صندوق سحب وإفلات بلا `input` وبلا معالج | `IntakePage.tsx:374-381` |
| أزرار التحميل | بلا `onClick` في الأدمن والبوابة | `DocumentsPage.tsx:82` |
| تتبع الحملات UTM | يُلتقط ثم **لا يُستخدم في أي مكان**، وتُعرض بيانات مزيفة بدلًا منه | `attribution.ts`, `LeadDetailPage.tsx:175` |
| المهام | الإنجاز يُحفظ في state محلي فقط ويضيع عند التحديث | `TasksPage.tsx:20-23` |
| السحب والإفلات في Pipeline | مكتبة `@dnd-kit` مثبتة لكن غير مستخدمة | `PipelinePage.tsx` |

## رابعًا: أزرار ومسارات ميتة

- لوحة التحكم: أزرار «إضافة قضية / حجز موعد / إضافة مهمة / إضافة طلب يدوي» كلها تنتهي
  لصفحات بلا نماذج إنشاء (`DashboardPage.tsx:80-85`).
- `/lawyer` و`/insights` يحوّلان إلى `/about` — صفحتا `LawyerPage` و`InsightsPage`
  **كاملتان لكن غير قابلتين للوصول**، ورابط «العودة للمقالات» في `ArticlePage.tsx:20` مكسور.
- ترحيب ثابت «مرحباً، أستاذ بن نوح المحامي» بغض النظر عن المسجل (`AdminLayout.tsx:320`).
- صور Unsplash خارجية في الصفحة الرئيسية (`HomePage.tsx`) — غير مناسبة للإنتاج.

---

## خامسًا: ما ينقص النظام كليًا ليكون متكاملًا

### Backend وبيانات (الأولوية القصوى)
1. **مصادقة Supabase حقيقية**: تسجيل دخول، استعادة كلمة مرور، ربط `auth.users` بـ `profiles`
   عبر trigger، ودعوات حسابات لعملاء البوابة.
2. **مسار كتابة/قراءة فعلي**: ربط كل الصفحات بـ Supabase بدل localStorage، تصحيح أسماء
   الأعمدة، وإظهار الأخطاء للمستخدم بدل ابتلاعها.
3. **إصلاح RLS**: سياسات لجداول `appointments`/`tasks`/`messages`، قفل الجداول المفتوحة،
   custom claims للأدوار.
4. **Supabase Storage**: حاوية ملفات + روابط موقعة + رفع فعلي في نموذج الاستقبال وصفحة المستندات.
5. **Edge Functions** (لا يوجد مجلد `supabase/functions`): لإرسال واتساب/البريد (مفاتيح
   `WHATSAPP_ACCESS_TOKEN` و`SUPABASE_SERVICE_ROLE_KEY` لا يجوز أن تكون في المتصفح)،
   والتذكيرات المجدولة (pg_cron)، ونقطة الذكاء الاصطناعي.
6. **جداول ناقصة في المخطط**: `activities` (سجل تدقيق — مهم قانونيًا لمكتب محاماة) و`notifications`.
7. **Realtime** بدل الاستطلاع الدوري المحلي، لمزامنة الإشعارات والرسائل بين المستخدمين.

### وظائف CRM غير موجودة
8. **الفوترة والمدفوعات**: لا فواتير، لا أسعار، لا دفع — رغم أن «الإيرادات» معروضة في التحليلات.
9. **إنشاء/تعديل/حذف** للقضايا والمهام والمستندات والعملاء (الأدمن اليوم يستطيع فقط
   تغيير مرحلة العميل المحتمل وإضافة ملاحظة).
10. **تقويم وإدارة مواعيد**: لا عرض تقويم، لا إعادة جدولة/إلغاء، لا منع حجز مزدوج،
    و`bookingSettings` (أيام العمل، التواريخ المحظورة) غير مستخدمة.
11. **صندوق رسائل للأدمن**: جدول `messages` موجود لكن لا واجهة لقراءة/الرد على رسائل العملاء.
12. **إدارة المستخدمين والفريق**: لا صفحة مستخدمين، لا دعوات، لا إسناد قضايا لمحامين.
13. **تقارير وتصدير**: التحليلات ثابتة؛ لا فلاتر تاريخ، لا تجميع حقيقي، لا تصدير CSV/PDF.
14. **توقيع إلكتروني وتوليد مستندات**: غير موجود إطلاقًا.

### جودة هندسية
15. **لا توجد اختبارات**: لا ملفات اختبار ولا أداة تشغيل في `package.json`.
16. **TypeScript غير صارم**: خيار `strict` غير مفعّل في `tsconfig.app.json`.
17. **خطأ فحص واحد**: Hook شرطي في `src/pages/admin/LeadDetailPage.tsx:45` — يجب إصلاحه فورًا.
18. **155 تحذير lint** أغلبها imports غير مستخدمة (تنظيف تجميلي).
19. **تحليلات وهمية**: `track()` مجرد `console.debug` في بيئة التطوير — لا GA/بكسل فعلي.
20. **SEO جزئي**: لا Open Graph/Twitter cards في `index.html`، ولا sitemap/robots.

---

## سادسًا: خطة العمل المقترحة بالترتيب

| المرحلة | العمل | الأثر |
|---|---|---|
| 1 | مصادقة Supabase حقيقية + إصلاح `profiles` + custom claims للأدوار | أساس كل شيء |
| 2 | ربط القراءة/الكتابة بـ Supabase وإزالة localStorage + إصلاح RLS | بيانات حقيقية مشتركة |
| 3 | إصلاح هوية البوابة (ربط العميل بالمستخدم) + فلترة بياناتها | بوابة تعمل وآمنة |
| 4 | Storage + رفع/تحميل المستندات | ميزة جوهرية لمكتب محاماة |
| 5 | Edge Functions: واتساب/بريد/تذكيرات/AI | تفعيل الأتمتة المحاكاة |
| 6 | CRUD كامل: قضايا، مهام، مستندات، عملاء + تقويم ومواعيد | اكتمال الـ CRM |
| 7 | الفوترة والمدفوعات | إيرادات فعلية |
| 8 | اختبارات + تفعيل `strict` + إصلاح خطأ الـ Hook + تنظيف التحذيرات | جاهزية إنتاج |
| 9 | تحليلات حقيقية + SEO + إزالة الصور الخارجية | تسويق وقياس |

**تقدير الوضع:** الواجهات ~85% مكتملة، الوظائف الفعلية ~25%. الفجوة كلها تقريبًا في الـ Backend والتكاملات.
